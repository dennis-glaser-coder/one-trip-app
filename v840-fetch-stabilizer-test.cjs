const fs=require('fs'),vm=require('vm');const code=fs.readFileSync('./noreyo-v840.js','utf8');let baseCalls=0;const base=async()=>{baseCalls++;return'ok'};const listeners={};const ctx={console,window:{fetch:base,addEventListener(type,fn){listeners[type]=fn}},Object,Array};vm.createContext(ctx);vm.runInContext(code,ctx);const a=ctx.window.NOREYO_V840,first=ctx.window.fetch;let fail=0;const markers=a.MARKERS.every(k=>first[k]===true)&&first.__noreyoV840===true;console.log(markers?'PASS V8.40 advertises V8.39 and legacy wrapper markers':'FAIL markers');if(!markers)fail++;listeners.pageshow?.();const same=ctx.window.fetch===first;console.log(same?'PASS pageshow does not grow fetch stack':'FAIL wrapper grew');if(!same)fail++;const has839=a.MARKERS.includes('__noreyoV839');console.log(has839?'PASS strict occupancy marker covered':'FAIL V839 marker missing');if(!has839)fail++;if(fail)process.exit(1);