const fs=require('fs'),vm=require('vm'),path=require('path');const code=fs.readFileSync(path.join(__dirname,'noreyo-v904.js'),'utf8');let persisted=0,counts=0;const ctx={console,Object,states:{Flug0:'must',Flug1:'wish',Hotel0:'must'},activeTab:'Flug',persistState(){persisted++},updateCounts(){counts++},window:{addEventListener(){}},document:{body:null,getElementById(){return null}},MutationObserver:function(){this.observe=()=>{};this.disconnect=()=>{}},requestAnimationFrame(){return 1},cancelAnimationFrame(){}};vm.createContext(ctx);vm.runInContext(code,ctx);const a=ctx.window.NOREYO_V904;let fail=0;let ok=JSON.stringify(a.flightKeys())==='["Flug0","Flug1"]';console.log(ok?'PASS flight keys isolated':'FAIL keys');if(!ok)fail++;ok=a.migrateMustToWish()===true&&ctx.states.Flug0==='wish'&&ctx.states.Hotel0==='must'&&persisted===1&&counts===1;console.log(ok?'PASS unsupported flight MUST migrates to wish only':'FAIL migrate');if(!ok)fail++;ok=a.migrateMustToWish()===false&&persisted===1;console.log(ok?'PASS migration idempotent':'FAIL idempotent');if(!ok)fail++;ctx.activeTab='Hotel';ok=a.activeFlight()===false;console.log(ok?'PASS non-flight tab untouched':'FAIL active tab');if(!ok)fail++;process.exit(fail?1:0);
