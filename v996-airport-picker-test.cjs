const fs=require('fs'),vm=require('vm');const code=fs.readFileSync('noreyo-v996.js','utf8');const ctx={console,String,Object,Array,searchState:{airports:['DUS','BER','MUC']},window:{addEventListener(){}},document:{getElementById(){return null}},MutationObserver:function(){this.observe=()=>{};this.disconnect=()=>{}},requestAnimationFrame(){return 1},cancelAnimationFrame(){}};vm.createContext(ctx);vm.runInContext(code,ctx);const a=ctx.window.NOREYO_V996;let fail=0;let codes=a.EXTRA.map(x=>x[0]);let ok=codes.includes('BER')&&codes.includes('HAM')&&codes.includes('MUC')&&codes.includes('STR')&&codes.includes('DTM')&&new Set(codes).size===codes.length;console.log(ok?'PASS expanded airport set includes major German origins without duplicates':'FAIL airports '+codes.join(','));if(!ok)fail++;const html=a.markup();ok=/data-noreyo-v996-airport="BER" aria-pressed="true"/.test(html)&&/data-noreyo-v996-airport="HAM" aria-pressed="false"/.test(html);console.log(ok?'PASS airport markup mirrors selected state':'FAIL markup');if(!ok)fail++;ok=a.esc('<script>')==='&lt;script&gt;';console.log(ok?'PASS airport labels are escaped':'FAIL escape');if(!ok)fail++;process.exit(fail?1:0);