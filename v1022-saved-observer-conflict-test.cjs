const fs=require('fs'),vm=require('vm');const code=fs.readFileSync('./noreyo-v1022.js','utf8');let cleanup=0,lists=0,detail=0,newSync=0;const legacy={cleanup(){cleanup++},fixSavedLists(){lists++;return true},fixSavedDetail(){detail++;return true}};const ctx={console,window:{addEventListener(){},NOREYO_V953:legacy,NOREYO_V1014:{sync(){newSync++;return true}}},document:{body:{}},MutationObserver:function(){this.observe=()=>{};this.disconnect=()=>{}},requestAnimationFrame(){return 1},cancelAnimationFrame(){}};vm.createContext(ctx);vm.runInContext(code,ctx);const a=ctx.window.NOREYO_V1022;let fail=0;a.sync();let ok=cleanup>=2&&lists===1&&detail===0&&newSync===1;console.log(ok?'PASS legacy detail writer stopped while list labels remain':'FAIL sync');if(!ok)fail++;const before=cleanup;a.observe();ok=cleanup>before;console.log(ok?'PASS observer rebind stops legacy observer first':'FAIL rebind');if(!ok)fail++;a.cleanup();ok=cleanup>before+1;console.log(ok?'PASS cleanup keeps legacy observer retired':'FAIL cleanup');if(!ok)fail++;process.exit(fail?1:0);