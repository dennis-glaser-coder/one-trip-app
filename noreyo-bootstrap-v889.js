/* NOREYO V8.89 bootstrap — preload corrected flightSearch guards before V8.87. */
(()=>{
'use strict';
const BUILD='8.89-safe',ATTEMPTS=2,RETRY_MS=260,TIMEOUT_MS=15000,READY_TIMEOUT_MS=45000,POLL_MS=25;
const KEY='__NOREYO_V889_SINGLE_FLIGHT__';
const COMPONENTS=Object.freeze([
  {name:'Flight guard compatibility V8.88',src:'./noreyo-v888.js?build=888',ready:()=>window.NOREYO_V888?.BUILD==='8.88'},
  {name:'V8.87',src:'./noreyo-bootstrap-v887.js?build=887',asyncReady:true,ready:()=>window.NOREYO_V887?.state?.().status==='ready'}
]);
function state(){const old=window[KEY];if(old&&old.status)return old;const s={status:'idle',promise:null,error:null,component:null,attempt:0};try{window[KEY]=s;}catch(_){}return s;}
function src(base,attempt){return attempt<=1?base:base+(base.includes('?')?'&':'?')+'noreyo_v889_retry='+attempt;}
function sleep(ms){return new Promise(r=>setTimeout(r,ms));}
async function waitReady(component){if(component.ready())return true;const start=Date.now();while(Date.now()-start<READY_TIMEOUT_MS){await sleep(POLL_MS);if(component.ready())return true;if(component.name==='V8.87'){const inner=window.NOREYO_V887?.state?.();if(inner?.status==='failed')throw Object.assign(inner.error||new Error('V8.87 inner failed'),{replaySafe:false});}}return false;}
function loadOnce(component,attempt){return new Promise((resolve,reject)=>{if(component.ready())return resolve(true);const s=document.createElement('script');let done=false,timer=0,loaded=false;const finish=(ok,error)=>{if(done)return;done=true;if(timer)clearTimeout(timer);s.onload=s.onerror=null;if(!ok&&!loaded){try{s.remove();}catch(_){}}ok?resolve(true):reject(error||new Error(component.name+' konnte nicht geladen werden'));};timer=setTimeout(()=>finish(false,Object.assign(new Error(component.name+' Netzwerk-/Script-Timeout'),{replaySafe:!loaded})),TIMEOUT_MS);s.src=src(component.src,attempt);s.onload=async()=>{loaded=true;if(timer){clearTimeout(timer);timer=0;}if(!component.asyncReady)return finish(component.ready(),Object.assign(new Error(component.name+' meldet keinen gültigen Build'),{replaySafe:false}));try{const ready=await waitReady(component);finish(ready,Object.assign(new Error(component.name+' wurde geladen, aber nicht vollständig bereit'),{replaySafe:false}));}catch(error){finish(false,Object.assign(error,{replaySafe:false}));}};s.onerror=()=>finish(false,Object.assign(new Error(component.name+' konnte nicht geladen werden'),{replaySafe:true}));document.head.appendChild(s);});}
async function loadComponent(component,st){if(component.ready())return true;st.component=component.name;let last=null;for(let attempt=1;attempt<=ATTEMPTS;attempt++){st.attempt=attempt;try{return await loadOnce(component,attempt);}catch(e){last=e;if(e?.replaySafe===false||attempt>=ATTEMPTS)break;await sleep(RETRY_MS);}}throw last;}
function fail(error){const status=document.getElementById('status'),bar=document.getElementById('bar'),box=document.getElementById('error');if(bar)bar.style.display='none';if(status)status.textContent='NOREYO konnte nicht geladen werden';if(box){box.style.display='block';box.setAttribute('role','alert');box.setAttribute('aria-live','assertive');if(!box.querySelector('[data-noreyo-v889-retry="1"]')){box.textContent='Die Verbindung zum Reisemodul ist fehlgeschlagen.';const br=document.createElement('br'),button=document.createElement('button');button.type='button';button.className='boot-retry';button.setAttribute('data-noreyo-v889-retry','1');button.textContent='Erneut versuchen';button.addEventListener('click',()=>location.reload());box.appendChild(br);box.appendChild(button);try{button.focus({preventScroll:true});}catch(_){}}}console.error(error);}
async function run(){const st=state();if(st.status==='ready')return true;if(st.promise)return st.promise;st.status='loading';st.error=null;st.promise=(async()=>{try{for(const c of COMPONENTS)await loadComponent(c,st);st.status='ready';st.component=null;st.attempt=0;return true;}catch(e){st.status='failed';st.error=e;fail(e);throw e;}})().finally(()=>{st.promise=null;});return st.promise;}
window.NOREYO_V889=Object.freeze({BUILD,ATTEMPTS,RETRY_MS,TIMEOUT_MS,READY_TIMEOUT_MS,POLL_MS,KEY,COMPONENTS,state,src,sleep,waitReady,loadOnce,loadComponent,run});run().catch(()=>{});
})();