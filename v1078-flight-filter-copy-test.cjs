const fs=require('fs'),vm=require('vm');const code=fs.readFileSync('./noreyo-v1078.js','utf8');const ctx={console,String,Object,window:{addEventListener(){}},document:{getElementById(){return null}},NodeFilter:{SHOW_TEXT:4},MutationObserver:function(){this.observe=()=>{};this.disconnect=()=>{}},requestAnimationFrame(){return 1},cancelAnimationFrame(){}};vm.createContext(ctx);vm.runInContext(code,ctx);const a=ctx.window.NOREYO_V1078;let fail=0;const stale='Diese Flugwünsche werden gespeichert. Die separate Flugsuche wendet sie aktuell noch nicht als harte Filter an.';let next=a.replaceText(stale),ok=/Pflichtmerkmal/.test(next)&&/nicht auswählbar/.test(next)&&!/noch nicht als harte Filter/.test(next);console.log(ok?'PASS stale flight-filter intro becomes current capability truth':'FAIL intro');if(!ok)fail++;const stale2='Wird gespeichert; aktuell noch nicht automatisch auf die separaten Flugangebote angewendet.';next=a.replaceText(stale2);ok=/Live-Flugsuche/.test(next)&&/Pflichtgrenzen/.test(next);console.log(ok?'PASS stale flight-limit hint becomes current enforcement truth':'FAIL hint');if(!ok)fail++;ok=a.replaceText('Hotelpreise werden live gesucht.')==='Hotelpreise werden live gesucht.';console.log(ok?'PASS unrelated copy remains unchanged':'FAIL unrelated');if(!ok)fail++;process.exit(fail?1:0);