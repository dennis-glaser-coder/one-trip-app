const fs=require('fs'),vm=require('vm');const code=fs.readFileSync('./noreyo-v849.js','utf8');const listeners={};const ctx={console,window:{fetch:async()=>1,addEventListener(t,f){listeners[t]=f}},Object,Array};vm.createContext(ctx);vm.runInContext(code,ctx);const a=ctx.window.NOREYO_V849,first=ctx.window.fetch;let fail=0;const ok=a.MARKERS.includes('__noreyoV848')&&first.__noreyoV848&&first.__noreyoV849;console.log(ok?'PASS V8.49 covers type-safe occupancy marker':'FAIL markers');if(!ok)fail++;listeners.pageshow?.();const same=ctx.window.fetch===first;console.log(same?'PASS V8.49 pageshow remains single wrapper':'FAIL stack grew');if(!same)fail++;if(fail)process.exit(1);