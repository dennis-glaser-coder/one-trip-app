const fs=require('fs'),vm=require('vm');const code=fs.readFileSync('./noreyo-v1062.js','utf8');let current='',shown=[];const offers={get textContent(){return current}},match={textContent:''};const ctx={console,String,window:{addEventListener(){},showToast(m){shown.push(m)}},document:{getElementById(id){return id==='offers'?offers:null},querySelector(){return match}}};vm.createContext(ctx);vm.runInContext(code,ctx);const a=ctx.window.NOREYO_V1062;let fail=0;const cases=[['Live-Suche konnte nicht geladen werden Netzwerkfehler','Suchfehlers'],['Keine vollständige Übereinstimmung Pflichtkriterien prüfen','Pflicht- oder Filterkriterien'],['Keine Verfügbarkeit gefunden Keine Rate für diesen Zeitraum','keine verfügbare Rate'],['Irgendetwas anderes','nicht verfügbar']];for(const [text,want] of cases){current=text;const got=a.translate(a.LEGACY),ok=got.includes(want);console.log((ok?'PASS ':'FAIL ')+text+' -> '+got);if(!ok)fail++;}current='Live-Suche unterbrochen';ctx.window.showToast(a.LEGACY);let ok=shown.at(-1).includes('Suchfehlers');console.log(ok?'PASS legacy detail toast is corrected at runtime':'FAIL runtime '+shown.at(-1));if(!ok)fail++;ctx.window.showToast('Favorit gespeichert');ok=shown.at(-1)==='Favorit gespeichert';console.log(ok?'PASS unrelated toast remains untouched':'FAIL unrelated');if(!ok)fail++;ok=a.install()===false;console.log(ok?'PASS toast wrapper installation idempotent':'FAIL idempotent');if(!ok)fail++;process.exit(fail?1:0);