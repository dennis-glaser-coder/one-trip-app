const fs=require('fs'),vm=require('vm');const code=fs.readFileSync('./noreyo-v857.js','utf8');let adds=0,removes=0;function Card(k){this.k=k;}Card.prototype.hasAttribute=function(n){return n==='data-v584-open'};Card.prototype.getAttribute=function(){return encodeURIComponent(this.k)};function List(cards=[]){this.children=cards;}List.prototype.replaceChildren=function(){this.children=[]};const list=new List([new Card('old')]),empty={style:{display:'none'}};const document={getElementById(id){return id==='favList'?list:id==='favEmpty'?empty:null},addEventListener(type){if(type==='click')adds++},removeEventListener(type){if(type==='click')removes++}};const ctx={console,Array,String,Object,decodeURIComponent,encodeURIComponent,document,window:{addEventListener(){},NOREYO_V584:{refreshFavorites(){}}},savedFavorites:[],requestAnimationFrame(){return 1},cancelAnimationFrame(){},setTimeout(fn){fn()},MutationObserver:function(){this.observe=()=>{};this.disconnect=()=>{}}};vm.createContext(ctx);vm.runInContext(code,ctx);const a=ctx.window.NOREYO_V857;let fail=0;a.bind();a.bind();let ok=adds===1&&a.bound===true;console.log(ok?'PASS repeated bind adds one click listener':'FAIL '+JSON.stringify({adds,bound:a.bound}));if(!ok)fail++;a.cleanup();ok=removes===1&&a.bound===false;console.log(ok?'PASS cleanup removes listener exactly once':'FAIL '+JSON.stringify({removes,bound:a.bound}));if(!ok)fail++;a.bind();ok=adds===2&&a.bound===true;console.log(ok?'PASS BFCache rebind adds one fresh listener':'FAIL '+JSON.stringify({adds,bound:a.bound}));if(!ok)fail++;ok=a.repair()===true&&list.children.length===0&&empty.style.display==='';console.log(ok?'PASS empty saved state still clears stale fallback':'FAIL stale');if(!ok)fail++;if(fail)process.exit(1);