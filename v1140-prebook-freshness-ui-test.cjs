const fs=require('fs'),vm=require('vm');const code=fs.readFileSync('./noreyo-v1140.js','utf8');
function El(text=''){this.textContent=text;this.attrs={};this.disabled=false;}El.prototype.getAttribute=function(k){return this.attrs[k]??null};El.prototype.setAttribute=function(k,v){this.attrs[k]=String(v)};
const btn=new El('Checkout-Session erneut live prüfen'),note=new El('Checkout-Session wurde live erneut bestätigt.'),root=new El();root.querySelector=sel=>sel.includes('session-check')?btn:sel.includes('session-note')?note:null;
let isFresh=false,ready=false;const ctx={console,Date,Number,String,Object,Math,setTimeout(){return 1},clearTimeout(){},window:{addEventListener(){},NOREYO_V1138:{MAX_AGE_MS:60000,fresh(){return isFresh}},NOREYO_V1128:{checkoutReady(){return ready}},NOREYO_V1136:{render(){return true}},NOREYO_HOTEL_PREBOOK_STATUS:{checkedAt:new Date().toISOString()}},document:{body:null,querySelector(sel){return sel==='.noreyo-v1128-ready'?root:null}},MutationObserver:function(){this.observe=()=>{};this.disconnect=()=>{}},requestAnimationFrame(){return 1},cancelAnimationFrame(){}};
vm.createContext(ctx);vm.runInContext(code,ctx);const a=ctx.window.NOREYO_V1140;let fail=0;
let ok=a.reconcile()===true&&btn.textContent==='Checkout-Session jetzt live prüfen'&&note.textContent.includes('nicht mehr frisch genug')&&root.attrs['data-checkout-ready']==='false';console.log(ok?'PASS stale PREBOOK status cannot display confirmed UI':'FAIL stale');if(!ok)fail++;
isFresh=true;ready=true;a.reconcile();ok=btn.textContent==='Checkout-Session erneut live prüfen'&&note.textContent.includes('vor weniger als 60 Sekunden')&&root.attrs['data-checkout-ready']==='true';console.log(ok?'PASS fresh PREBOOK status displays fresh confirmation truthfully':'FAIL fresh');if(!ok)fail++;
process.exit(fail?1:0);