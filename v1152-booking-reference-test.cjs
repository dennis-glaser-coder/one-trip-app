const fs=require('fs'),vm=require('vm');const code=fs.readFileSync(require('path').join(__dirname,'noreyo-v1152.js'),'utf8');let cleared=0;const base={sessionKey:'P1|O1|999|EUR',prebookId:'P1',holder:{firstName:'Dennis'},finalBinding:{price:'{"prebookId":"P1","priceCents":99900,"currency":"EUR"}',terms:'{"prebookId":"P1","policies":"A"}'}};const ctx={console,String,Object,Math,window:{addEventListener(){},NOREYO_HOTEL_BOOKING_DRAFT:Object.freeze({...base}),NOREYO_V1148:{clear(){cleared++;delete ctx.window.NOREYO_HOTEL_BOOKING_DRAFT;return true}}},document:{body:null},MutationObserver:function(){this.observe=()=>{};this.disconnect=()=>{}},requestAnimationFrame(){return 1},cancelAnimationFrame(){}};vm.createContext(ctx);vm.runInContext(code,ctx);const a=ctx.window.NOREYO_V1152;let fail=0;const ref=a.reference();let ok=a.valid(ref)&&ref===a.reference(Object.freeze({...base,holder:{firstName:'Other'}}));console.log(ok?'PASS idempotency reference is valid, deterministic and independent of guest PII':'FAIL reference '+ref);if(!ok)fail++;ok=a.upgrade()===true&&ctx.window.NOREYO_HOTEL_BOOKING_DRAFT.clientReference===ref;console.log(ok?'PASS prepared booking draft receives deterministic clientReference':'FAIL upgrade');if(!ok)fail++;ok=a.sync()===false&&cleared===0;console.log(ok?'PASS unchanged exact booking context preserves clientReference':'FAIL stable');if(!ok)fail++;ctx.window.NOREYO_HOTEL_BOOKING_DRAFT=Object.freeze({...ctx.window.NOREYO_HOTEL_BOOKING_DRAFT,finalBinding:{...base.finalBinding,terms:'{"prebookId":"P1","policies":"B"}'}});ok=a.sync()===true&&cleared===1&&!ctx.window.NOREYO_HOTEL_BOOKING_DRAFT;console.log(ok?'PASS changed final provider binding retires stale idempotency reference/draft':'FAIL mutation');if(!ok)fail++;process.exit(fail?1:0);