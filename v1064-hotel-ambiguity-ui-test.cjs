const fs=require('fs'),vm=require('vm');const code=fs.readFileSync('./noreyo-v1064.js','utf8');function El(text=''){this.textContent=text;this.dataset={};this.attrs={onclick:'searchTrips()'};this.listeners={};}El.prototype.removeAttribute=function(k){delete this.attrs[k]};El.prototype.addEventListener=function(k,fn){this.listeners[k]=fn};El.prototype.focus=function(){this.focused=true};const heading=new El('Live-Suche konnte nicht geladen werden'),button=new El('Erneut versuchen'),mb=new El('Live-Suche unterbrochen'),small=new El('Deine Suchangaben bleiben erhalten.'),input=new El();let text='„Hilton“ ist als alleiniger Hotelname nicht eindeutig genug. Gefunden wurde „Hilton Dubai Palm Jumeirah“.';const root={get textContent(){return text},querySelector(sel){return sel==='b'?heading:sel==='button.planner-save'?button:null}};const match={querySelector(sel){return sel==='b'?mb:sel==='small'?small:null}};let goCalls=0;const ctx={console,String,setTimeout(fn){fn()},go(){goCalls++},window:{addEventListener(){}},document:{getElementById(id){return id==='offers'?root:null},querySelector(sel){return sel==='#results .match'?match:sel==='.view.active .hotelQueryInput'?input:null}},MutationObserver:function(){this.observe=()=>{};this.disconnect=()=>{}},requestAnimationFrame(){return 1},cancelAnimationFrame(){}};vm.createContext(ctx);vm.runInContext(code,ctx);const a=ctx.window.NOREYO_V1064;let fail=0;let ok=a.repair()&&heading.textContent==='Hotelname nicht eindeutig'&&button.textContent==='Hotelname oder Ort ergänzen'&&!('onclick' in button.attrs)&&mb.textContent==='Hotelname präzisieren';console.log(ok?'PASS ambiguous resolver failure becomes input-repair UI':'FAIL repair');if(!ok)fail++;button.listeners.click();ok=goCalls===1&&input.focused===true;console.log(ok?'PASS ambiguity CTA returns to hotel input and focuses it':'FAIL focus');if(!ok)fail++;ok=a.repair()===false;console.log(ok?'PASS ambiguity UI repair is idempotent':'FAIL idempotent');if(!ok)fail++;text='Netzwerkfehler';heading.textContent='Live-Suche konnte nicht geladen werden';ok=a.repair()===false&&heading.textContent==='Live-Suche konnte nicht geladen werden';console.log(ok?'PASS actual backend errors remain backend errors':'FAIL backend');if(!ok)fail++;process.exit(fail?1:0);