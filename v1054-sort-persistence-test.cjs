const fs=require('fs'),vm=require('vm');const code=fs.readFileSync('./noreyo-v1054.js','utf8');let active='price',renders=0;const btn={dataset:{get sort(){return active}},getAttribute(){return active}};const ctx={console,Number,String,Array,Object,Map,offers:[{id:'a',price:300,rating:'8,2'},{id:'b',price:100,rating:'9.1'},{id:'c',price:200,rating:'7,5'}],recommendedOrder:['c','a','b'],document:{querySelector(sel){return sel.includes('active-sort')?btn:null}},window:{addEventListener(){},renderOffers(){renders++;}}};vm.createContext(ctx);vm.runInContext(code,ctx);const api=ctx.window.NOREYO_V1054;let fail=0;ctx.window.renderOffers();let order=ctx.offers.map(x=>x.id).join('');let ok=order==='bca'&&renders===1;console.log(ok?'PASS active price sort reapplied before render':'FAIL price '+order);if(!ok)fail++;ctx.offers=[{id:'a',price:300,rating:'8,2'},{id:'b',price:100,rating:'9.1'},{id:'c',price:200,rating:'7,5'}];active='rating';ctx.window.renderOffers();order=ctx.offers.map(x=>x.id).join('');ok=order==='bac';console.log(ok?'PASS active rating sort reapplied before render':'FAIL rating '+order);if(!ok)fail++;ctx.offers=[{id:'a'},{id:'b'},{id:'c'}];active='recommended';ctx.window.renderOffers();order=ctx.offers.map(x=>x.id).join('');ok=order==='cab';console.log(ok?'PASS recommendation order reapplied before render':'FAIL recommended '+order);if(!ok)fail++;ok=api.install()===false;console.log(ok?'PASS render wrapper installation idempotent':'FAIL idempotent');if(!ok)fail++;process.exit(fail?1:0);